############################################
# MediaWiki Configuration
############################################

# Base URL of the MediaWiki API endpoint
# Example:
#   https://wiki.example.org/api.php
MW_API_BASE_URL=https://wiki.local/api.php

# MediaWiki bot credentials
MW_BOT_USERNAME=botuser
MW_BOT_PASSWORD=CHANGE_ME


############################################
# OpenAI / LLM Configuration
############################################

# OpenAI API key used for:
# - Chat completions
# - Vector embeddings
OPENAI_API_KEY=sk-REPLACE_WITH_REAL_KEY

# Default chat model for LLM completions
OPENAI_MODEL=gpt-4o-mini

# Default embedding model
EMBEDDING_MODEL=text-embedding-3-large


############################################
# JWT Authentication Secrets (Bidirectional)
############################################
# IMPORTANT:
# - These MUST be long, random, and kept secret.
# - Minimum recommended length: 32 characters.
# - NEVER reuse these across environments.

# For verifying tokens FROM the MWAssistant extension
JWT_MW_TO_MCP_SECRET=REPLACE_WITH_LONG_RANDOM_SECRET_FROM_MW

# For signing tokens TO the MWAssistant extension
JWT_MCP_TO_MW_SECRET=REPLACE_WITH_LONG_RANDOM_SECRET_TO_MW


############################################
# Vector Index / Persistence
############################################

# FAISS index binary file
VECTOR_INDEX_PATH=/app/data/faiss_index.bin

# FAISS metadata JSON file
VECTOR_META_PATH=/app/data/index_meta.json


############################################
# Namespace Access Control
############################################

# Comma-separated list of MediaWiki namespace IDs
# allowed for public vector search
# Common examples:
#   0  = Main
#   14 = Category
#   3000 = Custom namespace
ALLOWED_NAMESPACES_PUBLIC=0,14
